class ball:numbers
{
         public int getballx()
     { 
     GetfieldLenght();
     var ballX = GetfieldLenght() / 2;
      return ballX;
     }
         public int getbally()
     {
      GetfieldWidth();
     var ballY = GetfieldWidth() / 2;
      return ballY;
     }
}